# DSB-start

Минимальный стартовый установщик DepSandbox для рабочих проектов.

Цель: сотрудник открывает терминал в корне своего проекта, запускает одну команду, вводит выданный MCP-токен в локальный скрытый prompt, а дальше установка идёт по актуальной защищённой инструкции с `https://mcp.dep1c.com/connect/`.

## Быстрый запуск

Откройте терминал в корне проекта, куда нужно подключить песочницу 1С, и выполните:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -Command "$u='https://raw.githubusercontent.com/KORUS-Consulting-GK-LLC/DSB-start/main/install.ps1'; $p=Join-Path $env:TEMP ('dsb-start-' + [guid]::NewGuid() + '.ps1'); Invoke-WebRequest -UseBasicParsing -Uri $u -OutFile $p; & $p"
```

Скрипт спросит токен в терминале. В чат агенту токен вставлять не нужно.

## Что делает установщик

1. Проверяет Git, Node.js 18+ и Python 3; если их нет, пытается поставить через `winget`.
2. Скрыто запрашивает MCP-токен у пользователя.
3. С этим токеном получает `install-plan.json` с `mcp.dep1c.com`.
4. Скачивает защищённый `bootstrap.mjs`, сверяет SHA-256 из install plan.
5. Передаёт токен bootstrap-скрипту через временный файл, а не через аргументы командной строки.
6. Удаляет временный файл с токеном после запуска.
7. Если конфигурация не выбрана автоматически, показывает список и просит выбрать одну, несколько, все или только базовые MCP.

## Параметры

```powershell
.\install.ps1 [-ProjectRoot C:\path\project] [-Client auto|codex|cursor] [-Configuration UT1152781,ERP_2_5_26_118] [-BaseOnly] [-AllConfigurations] [-DryRun]
```

Полезные варианты:

- `-Client codex` или `-Client cursor` — зафиксировать IDE, если автоопределение не подходит.
- `-Configuration UT1152781` — сразу выбрать пару, без интерактивного списка.
- `-BaseOnly` — подключить только базовые MCP без пары конфигурации.
- `-AllConfigurations` — подключить все активные пары из каталога.
- `-DryRun` — проверить маршрут без записи проектных файлов.

## Безопасность

- В этом репозитории нет токенов, внутренних портов, credentials и пользовательских конфигов.
- Токен не передаётся в URL или argv.
- Скрипт не отключает TLS-проверку и не правит `hosts`.
- Для PowerShell используется process-local `-ExecutionPolicy Bypass`, системная политика Windows не меняется.
- Настоящая инструкция, каталог конфигураций и bootstrap отдаются только после Bearer-аутентификации на `mcp.dep1c.com`.

## Для агента

Пользователю достаточно дать такую фразу:

```text
Подключи песочницу DepSandbox к текущему проекту. Запусти официальный DSB-start из GitHub, токен запроси локально в терминале, не проси вставлять токен в чат.
```

Агент должен открыть терминал в корне проекта и выполнить команду из раздела «Быстрый запуск». Если среда блокирует отправку Bearer-токена внешнему host из чата, это штатный сценарий: токен вводится локально в `install.ps1`.

## Проверка репозитория

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\check.ps1
```
